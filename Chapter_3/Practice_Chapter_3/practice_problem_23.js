// Variable declare using camelCase format

const userName = "Johan"; // Recommended format

console.log(userName);

const username = "Jovan"; // Not recommended format

console.log(username);