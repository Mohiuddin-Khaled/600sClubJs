// Make an array to store the capital names of some countries

// string type array

const cities = ['Dhaka', 'Washington DC', 'Landon', 'Tokyo', 'Beijing', 'Moscow'];

// length of array

console.log(cities.length);