// Make an array to store the color names of my surrounding environment

// string type array

const colors = ['Gray', 'White', 'Blue', 'Green', 'Yellow', 'Red'];

// length of array

console.log(colors.length);