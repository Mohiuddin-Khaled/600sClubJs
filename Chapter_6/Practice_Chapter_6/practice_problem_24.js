// Check element in the array

const names = ["Lake", "Cloud", "Rain", "Monsoon"];

// Specific element in the array
if (names.includes('Rain')) {
    console.log('I need umbrella');
}
else {
    console.log('No rain no pain');
}

