// create a function that return the first character of a string
function strChar(str) {
    return str[0];
}

// call the function by passing a string value as a parameter
const ch = strChar("Raju"); // Receive the first character of a string 
console.log(ch);