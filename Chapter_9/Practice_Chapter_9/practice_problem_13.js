// create a function to calculate the mean of four numbers
function fourMean(a, b, c, d) {
    return mean = (a + b + c + d) / 4;
};

// call the function through four numbers
console.log(fourMean(12, 14, 16, 19).toFixed(2));