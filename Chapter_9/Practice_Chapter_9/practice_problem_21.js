// create a function to return length of a string 
function strlen(str) {
    return str.length;
};

const str = "David Alter";
// call the function by passing string value as a parameter
console.log(strlen(str));