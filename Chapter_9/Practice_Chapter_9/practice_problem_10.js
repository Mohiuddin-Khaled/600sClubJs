// Create a function that sums the exam marks of three subjects 

function summation(num1, num2, num3) {
    const sum = num1 + num2 + num3;
    console.log(sum);
};

// call the function by passing numbers as parameter
summation(25, 34, 435);