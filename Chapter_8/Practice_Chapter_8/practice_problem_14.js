// Create a college object that contains three properties

const college = {
    name: "ndc",
    establish: 1949,
    groups: ['Science', 'Arts', 'Commerce'] // array of object
};

// Access a property of the collage using an array index
console.log(college.groups[1]); // dot notation