// Create a bike object that contains properties 

const bike = {
    brand: "Hero",
    price: 120000,
    model: "Splendor"
};

// log the property values in the console
const values = Object.values(bike);
console.log(values);
