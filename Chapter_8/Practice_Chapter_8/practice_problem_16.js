// practice 1 page 137

// Create a book object that contains three properties
const book = {
    name: 'Computer Programming',
    author: 'Tamim Shahriar Suben',
    price: '$5'
};

// log the object keys in the console
console.log(Object.keys(book));

// log the object values to the console
console.log(Object.values(book));