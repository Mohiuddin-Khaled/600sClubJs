// Create a player object that contains properties
const player = {
    name: "Messi",
    team: "Argentina",
    goals: 19
};

// log the property values in the console
const values = Object.values(player);
console.log(values);