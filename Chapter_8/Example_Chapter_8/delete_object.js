// delete specific property of an object

// create a person object
const person = {
    name: "Alice",
    age: 25,
    country: "Bangladesh"
};

// delete age properties
delete person.age;

// log the object properties in the console
console.log(person);