// declare a object

// empty object

/* 
const student = { };
*/

// declare a student object
const student = {
    name: "David",
    age: 15,
    class: 9,
    isSingle: false
};

// console.log(student);

// declare a phone object
const phone = {
    brand: "apple",
    price: 80000,
    color: "silver",
    version: 20.1
};

// console.log(phone);

// declare a juice object
const juice = {
    name: "Green Mango",
    price: 100,
    sugar: false
};

// console.log(juice);

// declare a car object
const car = {
    name: "BMW",
    wheels: 4,
    maxSpeed: 100,
    ac: true
};

// console.log(car);

// declare a subject object
const subject = {
    name: "Biology",
    teacher: "Alex",
    examDate: "30 Dec",
    chapters: ["first", "second", "third"], // array of object
    nextExam: { // object of object
        name: "Final Exam",
        marks: 100
    }
};

console.log(subject);