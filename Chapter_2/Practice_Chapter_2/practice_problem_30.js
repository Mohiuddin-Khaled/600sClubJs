// Using pencil
// Division operation

const prompt = require("prompt-sync")()
// const numbersPencil = parseInt(prompt('Enter the numbers of pencil: ');
// const eachTimes = parseInt(prompt("Enter the number of each times using pencil: "));

const numbersPencil = 10;
const eachTimes = 2;
const totalTimes = numbersPencil / eachTimes;
console.log(totalTimes);