// Division operation

// Quotient & Remainder

// const prompt = require("prompt-sync")()
// const dividend = parseFloat(prompt("Enter the dividend value: "));
// const divisor = parseFloat(prompt("Enter the divisor value: "));

const dividend = 8;
const divisor = 3;

const quotient = dividend / divisor;
console.log(parseInt(quotient));

const remainder = dividend % divisor;
console.log(remainder);