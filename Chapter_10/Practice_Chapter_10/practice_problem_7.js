// create a function that convert a distance from yards to meter

// 1 yards = 0.91 meter
function yardsToMeter(yards) {
    const meters = yards * 0.91;
    return meters;
};

// call the function by passing distance as a parameter
const meters = yardsToMeter(2343);
