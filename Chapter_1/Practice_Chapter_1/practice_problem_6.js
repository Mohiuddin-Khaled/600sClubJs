// Multiplication operation

const prompt = require('prompt-sync')()

let value = parseFloat(prompt("Enter the Amount: "))
let person = parseFloat(prompt("Enter the Persons: "))

console.log(value * person)