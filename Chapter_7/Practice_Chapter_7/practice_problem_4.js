// Create an array that contains favorite football players' names
const players = ["Messi", "Ronaldo", "Neymar", "Mbappe"];

// Each element access through the for-of loop
for (const player of players) {
    console.log(player);
}