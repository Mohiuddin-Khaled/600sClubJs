// Create an array that contains your favorite foods
const foods = ['Fried Rice', 'Samosa', 'Burger', 'Chicken Curry'];

// Each element access through the for-of loop
for (const food of foods) {
    console.log(food);
}