// practice 1 page 105

// Create an array that contains your favorite subjects
const subjects = ['Physics', 'Chemistry', 'Biology', 'Math', 'Higher Math'];

// Each element access through the for-of loop
for (const sub of subjects) {
    console.log(sub);
}