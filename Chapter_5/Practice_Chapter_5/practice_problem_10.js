// Eligibility check for job examination

const age = 28;
const workExperience = 3;
if (age < 30 && workExperience > 2) {
    console.log("A job seeker can take the exam!");
}
else {
    console.log("A job seeker can't take the exam!");
}