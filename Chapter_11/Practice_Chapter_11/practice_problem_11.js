// practice 1 page 191

// create a template string that displays the result of subtracting two variables

const a = 100;
const b = 20;
const result = `The subtract of ${a} and ${b} is ${a - b}`;
console.log(result);
