// create a template string that displays the full name from an object

const person = {
    firstName: "Amit",
    lastName: "Kumar"
};

console.log(`Full Name: ${person.firstName} ${person.lastName}`);
