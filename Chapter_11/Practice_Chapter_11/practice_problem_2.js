// create a function that deposit money from an account holder

function deposit(pay = 50) {
    let total = 0;
    return total += pay;
}

// call the function by pass money as a parameter
// console.log(deposit(500));

// call the function without pass money as a parameter
console.log(deposit());