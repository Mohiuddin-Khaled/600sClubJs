// create a function that return square value of a number

function squareNumber(num = 1) {
    return num * num;
};

// call the function pass value as a parameter
console.log(squareNumber(2));

// call the function pass value as a parameter
console.log(squareNumber());