// Comparison between two variables value
// String data types case sensitive values

const variableA = "hello";
const variableB = "Hello";

// Two variable values are same but case sensitive

console.log(variableA == variableB);

// Two variable data types are same but case sensitive

console.log(variableA === variableB);
