// Comparison between two friends to get the number in the exam result

const examNumber = 45;
const friendExamNumber = 97;

// console.log(examNumber > friendExamNumber);

// My friend got more better number than me
console.log(examNumber < friendExamNumber);