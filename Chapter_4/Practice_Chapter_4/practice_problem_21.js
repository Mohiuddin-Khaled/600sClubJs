// Comparison between two variables value
// Number and string data type values

const variableX = 15;

const variableY = "20";

// Two variable values are not equal

console.log(variableX != variableY);

// Two variable data types are not same

console.log(variableX !== variableY);